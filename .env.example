# Dev port
PORT=4300

# Replicate API (for hero / OG / blog image generation — optional)
# Resolution order at runtime: this var → Kontavio memory fallback → ask
REPLICATE_API_TOKEN=

# Future placeholders (not used yet — add when auth / payments land)
# DATABASE_URL=postgresql://user:password@localhost:5432/auswandern
# SESSION_SECRET=
# STRIPE_SECRET_KEY=
# RESEND_API_KEY=
